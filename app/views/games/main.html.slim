h1 game main page

p= @user.name
p= @user.role.name

table.table.table-border
  tbody
    tr
      th ユーザ名
      th ロール
    - @group.users.each do |user|
      - next if @user.id == user.id
      tr
        td= user.name
        td= user.role.display_name(@user.role_id)

.center
  p#ready_user_number 0/5
  button#ready.btn.btn-primary 準備完了

